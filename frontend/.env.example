# Backend API URL
VITE_API_URL=http://localhost:4000

# Mixpanel Analytics Token
# This is a public/client-side project token (not a secret).
# It's safe to expose in the bundle. Used for product analytics tracking.
# Leave blank to disable analytics.
VITE_MIXPANEL_TOKEN=
